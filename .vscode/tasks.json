{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test",
            "type": "shell",
            "command": "swipl -q -g main -t halt -s ./test/test.pl"
        }
        {
            "label": "install",
            "type": "shell",
            "command": "./install_as_package.bat"
        }
    ]
}